# Проверка V79 CLEAN RUNTIME

- `index.html` подключает только `app-v79-unified.js` и `styles-v79-unified.css`.
- Старые `app-v64…v78` и `styles-v64…v78` не подключаются.
- Service Worker кэширует только V79-файлы и удаляет старые кэши.
- Все 30 основных маршрутов прошли smoke-test без ошибок рендера.
- JavaScript прошёл `node --check`.
- Светлая палитра и тёмный текст принудительно зафиксированы на уровне V79.
- Данные читаются из `secondBrainOS.v1` и из IndexedDB V76; перед первой миграцией создаётся автоматическая резервная копия.
- Старые файлы можно оставить в репозитории: они не исполняются и не влияют на клики.
