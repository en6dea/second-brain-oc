# Second Brain OS — Category Delete Final Fix

Точечный фикс удаления категорий.

Что исправлено:
- удаление категорий теперь работает по названию, а не только по id;
- удаляются все дубли категории с тем же названием;
- связанные операции, покупки и бюджеты переводятся в «Без категории»;
- удалённые категории не возвращаются из операций/старых бюджетов после normalize/save;
- дизайн не менялся.

После загрузки откройте `/force-update.html`, затем обычный домен.
