# Second Brain OS · V63 STABILITY LOCK

Финальный стабилизатор поверх V63.

Исправлено:
- страницы больше не должны прыгать при рендере, кликах, сохранении категорий и переходах;
- старые `scrollTo(0,0)` из прошлых слоёв блокируются на время рендера;
- scroll сохраняется отдельно по каждому разделу;
- строки в «Категориях расходов» перестроены: дата, описание, сумма, категория и кнопки больше не сжимают друг друга;
- календарные строки и бейджи `tasks` стали кликабельными и ведут в задачи;
- отключены визуальные transform-анимации, которые давали ощущение скачка.

Сохранено:
- V63 Unified OS Upgrade;
- V62 AI Life Coach;
- V61 Smart Daily OS;
- текущие данные localStorage.
