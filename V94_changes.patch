--- a/index.html
+++ b/index.html
@@ -8,17 +8,17 @@
   <meta name="apple-mobile-web-app-status-bar-style" content="default">
   <meta name="apple-mobile-web-app-title" content="Second Brain OS">
   <meta name="application-name" content="Second Brain OS">
-  <meta name="second-brain-build" content="second-brain-space-v93-personal-assistant-20260724-r1">
+  <meta name="second-brain-build" content="second-brain-space-v94-csv-import-restored-20260724-r1">
   <meta http-equiv="Cache-Control" content="no-store,no-cache,must-revalidate,max-age=0">
   <title>Second Brain OS</title>
-  <link rel="manifest" href="manifest.webmanifest?v=v93-r1">
-  <link rel="icon" href="icon-192-v84.png?v=v93-r1">
-  <link rel="apple-touch-icon" href="icon-192-v84.png?v=v93-r1">
+  <link rel="manifest" href="manifest.webmanifest?v=v94-r1">
+  <link rel="icon" href="icon-192-v84.png?v=v94-r1">
+  <link rel="apple-touch-icon" href="icon-192-v84.png?v=v94-r1">
   <link rel="preconnect" href="https://fonts.googleapis.com">
   <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
   <link href="https://fonts.googleapis.com/css2?family=Onest:wght@400;500;600;700;800&display=swap" rel="stylesheet">
   <link rel="stylesheet" href="styles-v884-finance.css?v=v885-r1">
-  <link rel="stylesheet" href="styles-v91-stability.css?v=v93-r1">
+  <link rel="stylesheet" href="styles-v91-stability.css?v=v94-r1">
   <style>/* Second Brain OS V80 — readable premium design */
 :root{
   --v78-bg:#fbf9ff;
@@ -1427,7 +1427,7 @@
 .v885-recovery{min-height:100vh;display:grid;place-items:center;padding:28px;background:radial-gradient(circle at 10% 10%,rgba(124,86,232,.15),transparent 30%),radial-gradient(circle at 90% 18%,rgba(99,203,230,.12),transparent 28%),linear-gradient(145deg,#fbf9ff,#eef4ff);font-family:Onest,Inter,system-ui,sans-serif;color:#14162b}.v885-recovery-card{width:min(720px,100%);padding:36px;border:1px solid rgba(111,92,172,.16);border-radius:30px;background:rgba(255,255,255,.9);box-shadow:0 26px 80px rgba(33,38,78,.14);backdrop-filter:blur(18px)}.v885-recovery-mark{width:52px;height:52px;border-radius:17px;display:grid;place-items:center;background:#171a2d;color:#fff;font-weight:800;letter-spacing:.08em}.v885-recovery-kicker{margin:22px 0 8px;color:#7357d8;font-size:12px;font-weight:800;letter-spacing:.12em}.v885-recovery h1{margin:0 0 14px;font-size:clamp(30px,5vw,46px);line-height:1.06}.v885-recovery p{font-size:16px;line-height:1.6;color:#59647c}.v885-recovery code{padding:3px 7px;border-radius:8px;background:#f0ecff;color:#5f44c6}.v885-recovery-note{padding:14px 16px;border-radius:16px;background:#f8f7ff;border:1px solid rgba(124,86,232,.12)}.v885-recovery-actions{display:flex;gap:12px;flex-wrap:wrap;margin-top:24px}.v885-recovery :is(a,button){min-height:50px;padding:0 18px;border-radius:16px;font:inherit;font-weight:750;cursor:pointer;display:inline-flex;align-items:center;justify-content:center;text-decoration:none}.v885-recovery .primary{border:0;background:linear-gradient(135deg,#7c56e8,#6848d7);color:#fff;box-shadow:0 12px 26px rgba(104,72,215,.24)}.v885-recovery button{background:#fff;color:#5f44c6;border:1px solid #dcd3f4}@media(max-width:560px){.v885-recovery{padding:14px}.v885-recovery-card{padding:24px;border-radius:24px}.v885-recovery-actions>*{width:100%}}
 </style>
 </head>
-<body class="v80-booting" data-sbos-build="second-brain-space-v93-personal-assistant-20260724-r1">
+<body class="v80-booting" data-sbos-build="second-brain-space-v94-csv-import-restored-20260724-r1">
   <div id="app"></div>
   <div id="modal" class="modal"><div class="modal-card"><div class="modal-head"><h2 id="modalTitle">Окно</h2><button data-v78-action="close-modal" aria-label="Закрыть">×</button></div><div id="modalBody"></div></div></div>
   <div id="toast" class="toast"></div>
@@ -2243,9 +2243,9 @@
     const catStats=categoryData(state.settings.v78.categoryPeriod || bestExpensePeriod());
     state.settings.v78.operationPage = operationPage;
     state.settings.v78.operationType = operationType;
-    return `<section class="v78-page"><header class="v78-page-head"><div><button class="v78-back" data-v78-route="finance" type="button">← Финансы</button><span>Папка финансов</span><h1>Операции</h1><p>Полный список без обрезания. На одной странице отображается ${PAGE_SIZE} операций.</p></div><div><button class="v78-secondary" data-v866-action="open-finance-categories" type="button">Категории</button><button class="v78-secondary" data-v81-action="open-category-editor" type="button">Переименовать CSV</button><button class="v78-secondary" data-v78-action="export-csv" data-scope="filtered" type="button">⇩ Экспорт списка</button><button class="v78-primary" data-v79-action="open-record-form" data-type="operation" type="button">＋ Операция</button></div></header>
+    return `<section class="v78-page"><header class="v78-page-head"><div><button class="v78-back" data-v78-route="finance" type="button">← Финансы</button><span>Папка финансов</span><h1>Операции</h1><p>Полный список без обрезания. На одной странице отображается ${PAGE_SIZE} операций.</p></div><div><button class="v78-secondary" data-v866-action="open-finance-categories" type="button">Категории</button><button class="v78-secondary" data-v85-action="import-bank-csv" type="button">⇧ Импорт CSV из банка</button><button class="v78-secondary" data-v81-action="open-category-editor" type="button">Переименовать CSV</button><button class="v78-secondary" data-v78-action="export-csv" data-scope="filtered" type="button">⇩ Экспорт списка</button><button class="v78-primary" data-v79-action="open-record-form" data-type="operation" type="button">＋ Операция</button></div></header>
       <section class="v78-operation-toolbar"><div class="v78-segment">${[['all','Все'],['income','Доходы'],['expense','Расходы']].map(([key,label])=>`<button class="${operationType===key?'active':''}" data-v78-action="operation-type" data-type="${key}" type="button">${label}</button>`).join('')}</div><label>⌕<input data-v78-operation-search value="${safe(operationQuery)}" placeholder="Категория, сумма, комментарий..."></label><span>${filtered.length} ${plural(filtered.length,'операция','операции','операций')}</span></section>
-      <section class="v81-category-hint"><div><b>Редактирование категорий после CSV стало понятнее</b><p>Кнопка «Категории» управляет выпадающими списками доходов и расходов. «Переименовать CSV» меняет старое название сразу во всей выписке.</p></div><div class="v81-category-pills">${catStats.rows.slice(0,6).map(row=>`<button data-v81-action="open-category-editor" data-from="${safe(row.name)}" type="button">${safe(row.name)} <small>${Math.round(row.value)}</small></button>`).join('') || '<span>Категории появятся после расходных операций</span>'}</div></section>
+      <section class="v81-category-hint"><div><b>Редактирование категорий после CSV стало понятнее</b><p>«Импорт CSV из банка» загружает новую выписку с предварительной проверкой и удалением дублей. «Категории» управляют справочником, а «Переименовать CSV» меняет старое название сразу во всей выписке.</p></div><div class="v81-category-pills">${catStats.rows.slice(0,6).map(row=>`<button data-v81-action="open-category-editor" data-from="${safe(row.name)}" type="button">${safe(row.name)} <small>${Math.round(row.value)}</small></button>`).join('') || '<span>Категории появятся после расходных операций</span>'}</div></section>
       <section class="v78-operation-table"><header><span>Дата</span><span>Тип</span><span>Категория</span><span>Комментарий / счёт</span><span>Сумма</span><span></span></header>${visible.map(operationRow).join('') || '<div class="v78-empty-big">Операций по выбранному фильтру нет.</div>'}</section>
       <footer class="v78-pagination"><span>Страница ${operationPage} из ${pages}</span><div><button data-v78-action="operation-page" data-page="${Math.max(1,operationPage-1)}" ${operationPage===1?'disabled':''} type="button">←</button>${paginationButtons(operationPage,pages).map(page=>`<button class="${page===operationPage?'active':''}" data-v78-action="operation-page" data-page="${page}" type="button">${page}</button>`).join('')}<button data-v78-action="operation-page" data-page="${Math.min(pages,operationPage+1)}" ${operationPage===pages?'disabled':''} type="button">→</button></div></footer>
     </section>`;
@@ -3413,7 +3413,7 @@
   financeAnalyticsPage=function(){const period=state.settings.v78.categoryPeriod||bestExpensePeriod(),data=financePeriodData(period),months=v8611ConfiguredMonths(),prev=financePeriodData(previousPeriod(period)),categoryRows=v8611CategoryAnalysis(period),transferGroups=new Map();data.transfers.forEach(item=>{const key=clean(item.category)||'Нейтральное движение',row=transferGroups.get(key)||{name:key,value:0,count:0};row.value+=operationAmount(item);row.count++;transferGroups.set(key,row);});return `<section class="v78-page"><header class="v78-page-head"><div><button class="v78-back" data-v78-route="finance" type="button">← Финансы</button><span>Полная финансовая аналитика</span><h1>Движение денег</h1><p>Доходы, расходы, нейтральные переводы, лимиты, обязательность и динамика категорий.</p></div><div><select class="v78-select" data-v78-category-period>${months.map(key=>`<option value="${key}" ${key===period?'selected':''}>${v8610MonthLabel(key)}</option>`).join('')||`<option value="${monthKey(todayKey())}">Текущий месяц</option>`}</select><button class="v78-secondary" data-v866-action="open-finance-categories" type="button">Категории и лимиты</button><button class="v78-primary" data-v78-route="finance-operations" type="button">Операции</button></div></header><section class="v867-analytics-kpis v8611-analytics-kpis"><article><small>Доходы</small><b class="positive">${safe(moneyText(data.income))}</b><span>${data.incomes.length} операций</span></article><article><small>Все расходы</small><b class="negative">${safe(moneyText(data.expense))}</b><span>${data.expenses.length} операций</span></article><article><small>Чистый поток</small><b class="${data.net>=0?'positive':'negative'}">${data.net>=0?'+':''}${safe(moneyText(data.net))}</b><span>к прошлому: ${safe(moneyText(data.net-prev.net))}</span></article><article><small>Нейтральные движения</small><b>${safe(moneyText(data.transferVolume))}</b><span>${data.transfers.length} переводов / возвратов</span></article><article><small>Норма сбережений</small><b class="${data.savingsRate>=0?'positive':'negative'}">${data.savingsRate}%</b><span>чистый поток / доходы</span></article></section><section class="v867-necessity-analysis"><button class="v8611-click-card mandatory" data-v8611-action="open-expense-class" data-value="mandatory" type="button"><header><i>🛡</i><div><small>Обязательные расходы</small><h2>${safe(moneyText(data.mandatory))}</h2></div></header><p>Провалитесь внутрь и проверьте, правильно ли каждой операции присвоен обязательный тип.</p><footer><b>${data.expense?Math.round(data.mandatory/data.expense*100):0}% расходов</b><span>${data.income?Math.round(data.mandatory/data.income*100):0}% дохода</span></footer></button><button class="v8611-click-card optional" data-v8611-action="open-expense-class" data-value="optional" type="button"><header><i>✨</i><div><small>Необязательные расходы</small><h2>${safe(moneyText(data.optional))}</h2></div></header><p>Откройте список покупок и развлечений, которые можно перенести, сократить или заменить.</p><footer><b>${data.expense?Math.round(data.optional/data.expense*100):0}% расходов</b><span>${data.income?Math.round(data.optional/data.income*100):0}% дохода</span></footer></button><button class="v8611-click-card limits" data-v8611-action="open-limit-table" type="button"><header><i>◌</i><div><small>Общий лимит категорий</small><h2>${safe(moneyText(data.totalLimit))}</h2></div></header><p>Потрачено ${safe(moneyText(data.limitSpent))}. Осталось ${safe(moneyText(Math.max(0,data.limitRemaining)))}.</p><footer><b>${data.totalLimit?Math.round(data.limitSpent/data.totalLimit*100):0}% использовано</b><span>${data.over.length} превышено</span></footer></button></section>${financeAssistantHtml(period,8)}<section class="v867-category-budget" id="v8611-category-budget"><header><div><h2>Категории, лимиты и динамика</h2><p>Лимит редактируется прямо в строке и сохраняется отдельно для выбранного месяца.</p></div><button data-v8611-action="auto-allocate-limits" data-period="${period}" type="button">Распределить по доходу</button></header><div class="v867-budget-table"><div class="head"><span>Категория</span><span>Тип</span><span>Расход</span><span>Лимит месяца</span><span>Использовано</span><span>Остаток</span><span>К прошлому</span></div>${categoryRows.map(row=>`<article class="${row.limit&&row.spent>row.limit?'over':row.percent>=80?'near':''}"><span><i style="background:${safe(row.category.color)}20;color:${safe(row.category.color)}">${safe(row.category.icon)}</i><b>${safe(row.category.name)}</b><small>${row.count} ${plural(row.count,'операция','операции','операций')}</small></span><span><mark class="v867-necessity ${row.category.defaultExpenseClass}">${expenseClassLabel(row.category.defaultExpenseClass)}</mark></span><strong>${safe(moneyText(row.spent))}</strong><span><div class="v8611-inline-limit"><input type="number" min="0" step="100" value="${row.limit||''}" data-v8611-limit-input="${safe(row.category.id)}" placeholder="Не задан"><button data-v8611-action="save-inline-limit" data-id="${safe(row.category.id)}" data-period="${period}" type="button">✓</button></div><small class="v8611-limit-note">для ${safe(v8610MonthLabel(period))}</small></span><span><u><b style="width:${Math.min(100,row.percent)}%"></b></u><em>${row.limit?`${row.percent}%`:'—'}</em></span><span class="${row.limit&&row.remaining<0?'negative':''}">${row.limit?safe(moneyText(row.remaining)):'—'}</span><span class="${row.spent-row.old>0?'negative':row.spent-row.old<0?'positive':''}">${row.spent-row.old>0?'+':''}${safe(moneyText(row.spent-row.old))}</span></article>`).join('')||'<div class="v78-empty-big">Задайте лимиты категориям или добавьте расходы.</div>'}</div></section>${transferGroups.size?`<section class="v8611-neutral-section"><header><div><h2>Нейтральные движения денег</h2><p>Переводы между счетами, возвраты и прочие движения исключены из доходов и расходов.</p></div><button data-v8611-action="open-transfer-operations" type="button">Открыть операции</button></header><div class="v8611-neutral-list">${[...transferGroups.values()].sort((a,b)=>b.value-a.value).map(row=>`<article><span><b>${safe(row.name)}</b><small>${row.count} ${plural(row.count,'операция','операции','операций')}</small></span><strong>${safe(moneyText(row.value))}</strong><span class="v8611-neutral-badge">не влияет на поток</span></article>`).join('')}</div></section>`:''}<section class="v867-cashflow-history"><header><h2>Движение денег по месяцам</h2><p>Доходы и расходы без внутренних переводов.</p></header><div>${months.slice().reverse().slice(-6).map(key=>{const row=financePeriodData(key),max=Math.max(row.income,row.expense,1);return `<article><span><b>${new Date(`${key}-01T12:00:00`).toLocaleDateString('ru-RU',{month:'short',year:'2-digit'})}</b><small class="${row.net>=0?'positive':'negative'}">${row.net>=0?'+':''}${safe(moneyText(row.net))}</small></span><div><i class="income" style="width:${row.income/max*100}%"></i><i class="expense" style="width:${row.expense/max*100}%"></i></div><em><small>Доход ${safe(moneyText(row.income))}</small><small>Расход ${safe(moneyText(row.expense))}</small></em></article>`}).join('')}</div></section></section>`;};
 
   operationRow=function(item){const kind=operationKind(item),necessity=kind==='expense'?expenseClass(item):'';return `<article class="${kind==='transfer'?'v8611-neutral-row':''}"><span>${safe(formatShort(item.date))}</span><span><i class="${kind}">${v8611FlowIcon(kind)}</i>${v8611FlowLabel(kind)}</span><span><b>${safe(item.category||'Без категории')}</b><small>${kind==='expense'?`<mark class="v867-necessity ${necessity}">${expenseClassLabel(necessity)}</mark>`:kind==='transfer'?'<mark class="v8611-neutral-badge">нейтральное движение</mark>':'доходная запись'}</small></span><span><b>${safe(item.note||'Без комментария')}</b><small>${safe(item.account||item.incomeSource||'')}</small></span><strong class="${kind==='income'?'positive':kind==='expense'?'negative':''}">${v8611FlowSign(kind)} ${safe(moneyText(item.amount))}</strong><span class="v78-row-menu v81-row-menu"><button data-v81-action="quick-category" data-id="${safe(item.id)}" type="button">Категория</button><button data-v79-action="edit-record" data-type="operation" data-id="${safe(item.id)}" type="button">Изменить</button><button class="danger" data-v79-action="delete-record" data-type="operation" data-id="${safe(item.id)}" type="button">Удалить</button></span></article>`;};
-  financeOperationsPage=function(){const all=array('operations').slice().sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.createdAt||'').localeCompare(String(a.createdAt||'')));let filtered=all.filter(item=>operationType==='all'||operationKind(item)===operationType);if(v8611OperationClass!=='all')filtered=filtered.filter(item=>operationKind(item)==='expense'&&expenseClass(item)===v8611OperationClass);if(operationQuery){const q=operationQuery.toLowerCase();filtered=filtered.filter(item=>[item.date,item.type,item.amount,item.category,item.account,item.note,item.incomeSource].some(value=>String(value||'').toLowerCase().includes(q)));}const pages=Math.max(1,Math.ceil(filtered.length/PAGE_SIZE));operationPage=Math.min(Math.max(1,operationPage),pages);const visible=filtered.slice((operationPage-1)*PAGE_SIZE,operationPage*PAGE_SIZE),stats=categoryData(state.settings.v78.categoryPeriod||bestExpensePeriod());return `<section class="v78-page"><header class="v78-page-head"><div><button class="v78-back" data-v78-route="finance" type="button">← Финансы</button><span>Папка финансов</span><h1>Операции</h1><p>Доходы, реальные расходы и нейтральные переводы учитываются раздельно.</p></div><div><button class="v78-secondary" data-v866-action="open-finance-categories" type="button">Категории</button><button class="v78-secondary" data-v81-action="open-category-editor" type="button">Переименовать CSV</button><button class="v78-secondary" data-v78-action="export-csv" data-scope="filtered" type="button">⇩ Экспорт</button><button class="v78-primary" data-v79-action="open-record-form" data-type="operation" type="button">＋ Операция</button></div></header><section class="v78-operation-toolbar"><div class="v78-segment">${[['all','Все'],['income','Доходы'],['expense','Расходы'],['transfer','Переводы']].map(([key,label])=>`<button class="${operationType===key?'active':''}" data-v78-action="operation-type" data-type="${key}" type="button">${label}</button>`).join('')}</div><label>⌕<input data-v78-operation-search value="${safe(operationQuery)}" placeholder="Категория, сумма, комментарий..."></label><span>${filtered.length} ${plural(filtered.length,'операция','операции','операций')}</span></section><section class="v8611-filter-row"><span>Характер расхода</span><div class="v78-segment">${[['all','Все расходы'],['mandatory','Обязательные'],['optional','Необязательные']].map(([key,label])=>`<button class="${v8611OperationClass===key?'active':''}" data-v8611-action="operation-class-filter" data-value="${key}" type="button">${label}</button>`).join('')}</div></section><section class="v81-category-hint"><div><b>Корректное движение денег</b><p>Переводы между своими счетами и возвраты вынесены в нейтральные движения и не увеличивают доходы или расходы.</p></div><div class="v81-category-pills">${stats.rows.slice(0,6).map(row=>`<button data-v81-action="open-category-editor" data-from="${safe(row.name)}" type="button">${safe(row.name)} <small>${Math.round(row.value)}</small></button>`).join('')||'<span>Категории появятся после расходов</span>'}</div></section><section class="v78-operation-table"><header><span>Дата</span><span>Тип</span><span>Категория</span><span>Комментарий / счёт</span><span>Сумма</span><span></span></header>${visible.map(operationRow).join('')||'<div class="v78-empty-big">Операций по выбранному фильтру нет.</div>'}</section><footer class="v78-pagination"><span>Страница ${operationPage} из ${pages}</span><div><button data-v78-action="operation-page" data-page="${Math.max(1,operationPage-1)}" ${operationPage===1?'disabled':''} type="button">←</button>${paginationButtons(operationPage,pages).map(page=>`<button class="${page===operationPage?'active':''}" data-v78-action="operation-page" data-page="${page}" type="button">${page}</button>`).join('')}<button data-v78-action="operation-page" data-page="${Math.min(pages,operationPage+1)}" ${operationPage===pages?'disabled':''} type="button">→</button></div></footer></section>`;};
+  financeOperationsPage=function(){const all=array('operations').slice().sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))||String(b.createdAt||'').localeCompare(String(a.createdAt||'')));let filtered=all.filter(item=>operationType==='all'||operationKind(item)===operationType);if(v8611OperationClass!=='all')filtered=filtered.filter(item=>operationKind(item)==='expense'&&expenseClass(item)===v8611OperationClass);if(operationQuery){const q=operationQuery.toLowerCase();filtered=filtered.filter(item=>[item.date,item.type,item.amount,item.category,item.account,item.note,item.incomeSource].some(value=>String(value||'').toLowerCase().includes(q)));}const pages=Math.max(1,Math.ceil(filtered.length/PAGE_SIZE));operationPage=Math.min(Math.max(1,operationPage),pages);const visible=filtered.slice((operationPage-1)*PAGE_SIZE,operationPage*PAGE_SIZE),stats=categoryData(state.settings.v78.categoryPeriod||bestExpensePeriod());return `<section class="v78-page"><header class="v78-page-head"><div><button class="v78-back" data-v78-route="finance" type="button">← Финансы</button><span>Папка финансов</span><h1>Операции</h1><p>Доходы, реальные расходы и нейтральные переводы учитываются раздельно.</p></div><div><button class="v78-secondary" data-v866-action="open-finance-categories" type="button">Категории</button><button class="v78-secondary" data-v85-action="import-bank-csv" type="button">⇧ Импорт CSV из банка</button><button class="v78-secondary" data-v81-action="open-category-editor" type="button">Переименовать CSV</button><button class="v78-secondary" data-v78-action="export-csv" data-scope="filtered" type="button">⇩ Экспорт</button><button class="v78-primary" data-v79-action="open-record-form" data-type="operation" type="button">＋ Операция</button></div></header><section class="v78-operation-toolbar"><div class="v78-segment">${[['all','Все'],['income','Доходы'],['expense','Расходы'],['transfer','Переводы']].map(([key,label])=>`<button class="${operationType===key?'active':''}" data-v78-action="operation-type" data-type="${key}" type="button">${label}</button>`).join('')}</div><label>⌕<input data-v78-operation-search value="${safe(operationQuery)}" placeholder="Категория, сумма, комментарий..."></label><span>${filtered.length} ${plural(filtered.length,'операция','операции','операций')}</span></section><section class="v8611-filter-row"><span>Характер расхода</span><div class="v78-segment">${[['all','Все расходы'],['mandatory','Обязательные'],['optional','Необязательные']].map(([key,label])=>`<button class="${v8611OperationClass===key?'active':''}" data-v8611-action="operation-class-filter" data-value="${key}" type="button">${label}</button>`).join('')}</div></section><section class="v81-category-hint"><div><b>Корректное движение денег</b><p>Переводы между своими счетами и возвраты вынесены в нейтральные движения и не увеличивают доходы или расходы.</p></div><div class="v81-category-pills">${stats.rows.slice(0,6).map(row=>`<button data-v81-action="open-category-editor" data-from="${safe(row.name)}" type="button">${safe(row.name)} <small>${Math.round(row.value)}</small></button>`).join('')||'<span>Категории появятся после расходов</span>'}</div></section><section class="v78-operation-table"><header><span>Дата</span><span>Тип</span><span>Категория</span><span>Комментарий / счёт</span><span>Сумма</span><span></span></header>${visible.map(operationRow).join('')||'<div class="v78-empty-big">Операций по выбранному фильтру нет.</div>'}</section><footer class="v78-pagination"><span>Страница ${operationPage} из ${pages}</span><div><button data-v78-action="operation-page" data-page="${Math.max(1,operationPage-1)}" ${operationPage===1?'disabled':''} type="button">←</button>${paginationButtons(operationPage,pages).map(page=>`<button class="${page===operationPage?'active':''}" data-v78-action="operation-page" data-page="${page}" type="button">${page}</button>`).join('')}<button data-v78-action="operation-page" data-page="${Math.min(pages,operationPage+1)}" ${operationPage===pages?'disabled':''} type="button">→</button></div></footer></section>`;};
   operationsFolderMini=function(){const ops=array('operations'),income=ops.filter(i=>operationKind(i)==='income').length,expense=ops.filter(i=>operationKind(i)==='expense').length,transfer=ops.filter(i=>operationKind(i)==='transfer').length;return card('Папка операций','Все финансовые записи в одном месте',`<div class="v78-operation-folder"><button data-v78-route="finance-operations" type="button"><i>▦</i><span>Все операции</span><b>${ops.length}</b></button><button data-v78-route="finance-operations" data-v78-filter="income" type="button"><i>↗</i><span>Доходы</span><b>${income}</b></button><button data-v78-route="finance-operations" data-v78-filter="expense" type="button"><i>↘</i><span>Расходы</span><b>${expense}</b></button><button data-v8611-action="open-transfer-operations" type="button"><i>⇄</i><span>Переводы</span><b>${transfer}</b></button><button data-v79-action="open-record-form" data-type="operation" type="button"><i>＋</i><span>Новая операция</span><b>добавить</b></button></div>`,'v78-operations-folder');};
 
   function v8611CaptureCategoryScroll(){const body=document.getElementById('modalBody'),card=document.querySelector('#modal .modal-card'),lists=[...document.querySelectorAll('.v866-category-list')];v8611CategoryScrollState={body:body?.scrollTop||0,card:card?.scrollTop||0,lists:lists.map(x=>x.scrollTop||0)};}
@@ -3791,7 +3791,7 @@
   /* Second Brain OS V88.5 — stable finance core.
      Only additive migrations. Existing collections are never cleared or replaced. */
   {
-    const V884_BUILD='second-brain-space-v93-personal-assistant-20260724-r1';
+    const V884_BUILD='second-brain-space-v94-csv-import-restored-20260724-r1';
     const V884_LABEL='V91 · FOCUSED HOTFIX';
     const V884_DAY=86400000;
     let v884ScrollY=null;
@@ -4045,7 +4045,7 @@
 (()=>{let started=false;const start=()=>{if(started)return;started=true;
 /* Second Brain OS V88 — visual runtime only. Reads state, never migrates or deletes data. */
 (() => {
-  const VISUAL_BUILD = 'second-brain-space-v93-personal-assistant-20260724-r1';
+  const VISUAL_BUILD = 'second-brain-space-v94-csv-import-restored-20260724-r1';
   const BUILD_LABEL = 'V91 · FOCUSED HOTFIX';
   const THEME_KEY = 'secondBrainOS.visualThemeMode';
   const LEGACY_THEME_KEY = 'secondBrainOS.visualTheme';
@@ -4568,7 +4568,7 @@
   <script defer src="https://www.gstatic.com/firebasejs/8.10.1/firebase-firestore.js"></script>
   <script defer src="firebase-config.js?v=v8612-r1"></script>
   <script defer src="cloud-sync.js?v=v8612-r1"></script>
-  <script defer src="pwa-v88.js?v=v885-r1"></script>
+  <script defer src="pwa-v88.js?v=v94-r1"></script>
 
   <style id="sbos-v8851-hotfix-style">
     html.sbos-modal-open, body.sbos-modal-open { overflow:hidden!important; }
@@ -4773,11 +4773,11 @@
   })();
   </script>
 
-  <link rel="stylesheet" href="styles-v91-stability.css?v=v93-r1" data-sbos-v91-final>
-  <script src="app-v91-stability.js?v=v93-r1"></script>
+  <link rel="stylesheet" href="styles-v91-stability.css?v=v94-r1" data-sbos-v91-final>
+  <script src="app-v91-stability.js?v=v94-r1"></script>
 
-  <link rel="stylesheet" href="styles-v93-assistant.css?v=v93-r1" data-sbos-v93>
-  <script src="app-v93-assistant.js?v=v93-r1" data-sbos-v93></script>
+  <link rel="stylesheet" href="styles-v93-assistant.css?v=v94-r1" data-sbos-v93>
+  <script src="app-v93-assistant.js?v=v94-r1" data-sbos-v93></script>
 
 </body>
 </html>
--- a/app-v93-assistant.js
+++ b/app-v93-assistant.js
@@ -1,11 +1,11 @@
 'use strict';
-/* Second Brain OS V93 — assistant UX, finance wizard and visual consistency layer. */
+/* Second Brain OS V94 — CSV bank import restored in Operations. */
 (() => {
-  const BUILD = 'V93 · PERSONAL ASSISTANT';
-  const VERSION = 'second-brain-space-v93-personal-assistant-20260724-r1';
+  const BUILD = 'V94 · CSV IMPORT RESTORED';
+  const VERSION = 'second-brain-space-v94-csv-import-restored-20260724-r1';
   const STORE_KEY = 'secondBrainOS.v1';
-  const BACKUP_MARK = 'secondBrainOS.v93.backupCreated';
-  const RAW_BACKUP = 'secondBrainOS.v93.rawLocalBackup';
+  const BACKUP_MARK = 'secondBrainOS.v94.backupCreated';
+  const RAW_BACKUP = 'secondBrainOS.v94.rawLocalBackup';
   const DB_NAME = 'SecondBrainOSDurableStorage';
   const DB_STORE = 'records';
   const DAY = 86400000;
@@ -98,10 +98,10 @@
       if (raw && !localStorage.getItem(RAW_BACKUP)) localStorage.setItem(RAW_BACKUP, raw);
       const snapshot = clone(state());
       if (snapshot) {
-        await dbPut(`backup:v93-before-personal-assistant:${at}`, {
+        await dbPut(`backup:v94-before-csv-import-restored:${at}`, {
           version: 93,
           createdAt: at,
-          reason: 'automatic-before-v93-personal-assistant',
+          reason: 'automatic-before-v94-csv-import-restored',
           state: snapshot
         });
       }
@@ -692,7 +692,7 @@
       overdue: (s.tasks || []).filter(item => item && !isDone(item) && item.date && item.date < today()).length
     };
     const backupAt = (() => { try { return localStorage.getItem(BACKUP_MARK); } catch (_) { return ''; } })();
-    page.insertAdjacentHTML('beforeend', `<section class="sbos-v93-quality-card"><header><div><small>КАЧЕСТВО СИСТЕМЫ</small><h2>Где базе не хватает ясности</h2><p>Показатели помогают контролировать не только задачи, но и качество самого Second Brain.</p></div></header><div><article><b>${counts.operations}</b><span>операций без категории</span></article><article><b>${counts.goals}</b><span>целей без следующего шага</span></article><article><b>${counts.debts}</b><span>долгов без даты или платежа</span></article><article><b>${counts.habits}</b><span>привычек без связи с целью</span></article><article><b>${counts.overdue}</b><span>просроченных задач</span></article><article><b>${backupAt ? new Date(backupAt).toLocaleDateString('ru-RU') : '—'}</b><span>последний backup V93</span></article></div></section>`);
+    page.insertAdjacentHTML('beforeend', `<section class="sbos-v93-quality-card"><header><div><small>КАЧЕСТВО СИСТЕМЫ</small><h2>Где базе не хватает ясности</h2><p>Показатели помогают контролировать не только задачи, но и качество самого Second Brain.</p></div></header><div><article><b>${counts.operations}</b><span>операций без категории</span></article><article><b>${counts.goals}</b><span>целей без следующего шага</span></article><article><b>${counts.debts}</b><span>долгов без даты или платежа</span></article><article><b>${counts.habits}</b><span>привычек без связи с целью</span></article><article><b>${counts.overdue}</b><span>просроченных задач</span></article><article><b>${backupAt ? new Date(backupAt).toLocaleDateString('ru-RU') : '—'}</b><span>последний backup V94</span></article></div></section>`);
   }
 
   function decorateRoundedSurfaces() {
--- a/styles-v93-assistant.css
+++ b/styles-v93-assistant.css
@@ -1,4 +1,4 @@
-/* Second Brain OS V93 — personal assistant, compact GameLife and visual consistency. */
+/* Second Brain OS V94 — CSV import restored in Operations; V93 assistant styling retained. */
 :root{
   --v93-ink:#18223b;
   --v93-muted:#66708a;
@@ -195,7 +195,7 @@
 /* The legacy V91 runtime still rewrites the text node after route renders.
    Keep the visible build marker truthful without mutating or disabling legacy code. */
 html body.sbos-v93-ready.v88-constellation .v78-build{font-size:0!important;line-height:1!important;color:transparent!important}
-html body.sbos-v93-ready.v88-constellation .v78-build::after{content:"V93 · PERSONAL ASSISTANT";font-size:8px!important;line-height:1.2!important;letter-spacing:.04em!important;color:#fff!important}
+html body.sbos-v93-ready.v88-constellation .v78-build::after{content:"V94 · CSV IMPORT RESTORED";font-size:8px!important;line-height:1.2!important;letter-spacing:.04em!important;color:#fff!important}
 
 /* Compact capture control: remains universally available without covering cards. */
 body.sbos-v93-ready .v86-capture-fab.sbos-v93-capture-fab{
--- a/pwa-v88.js
+++ b/pwa-v88.js
@@ -7,10 +7,10 @@
   function announceUpdate(){state.updateReady=true;window.dispatchEvent(new CustomEvent('second-brain-update-ready'));toast('Доступно безопасное обновление');render();}
   window.addEventListener('beforeinstallprompt',event=>{event.preventDefault();deferredPrompt=event;render();});
   window.addEventListener('appinstalled',()=>{state.installed=true;deferredPrompt=null;toast('Second Brain OS установлен');render();});
-  async function register(){if(!('serviceWorker' in navigator))return;try{const registration=await navigator.serviceWorker.register('./sw.js?v=v93-r1',{scope:'./',updateViaCache:'none'});state.registered=true;registration.addEventListener('updatefound',()=>{const worker=registration.installing;if(!worker)return;worker.addEventListener('statechange',()=>{if(worker.state==='installed'&&navigator.serviceWorker.controller)announceUpdate();});});if(registration.waiting&&navigator.serviceWorker.controller)announceUpdate();await registration.update().catch(()=>undefined);}catch(error){console.warn('[PWA V93]',error);}}
+  async function register(){if(!('serviceWorker' in navigator))return;try{const registration=await navigator.serviceWorker.register('./sw.js?v=v94-r1',{scope:'./',updateViaCache:'none'});state.registered=true;registration.addEventListener('updatefound',()=>{const worker=registration.installing;if(!worker)return;worker.addEventListener('statechange',()=>{if(worker.state==='installed'&&navigator.serviceWorker.controller)announceUpdate();});});if(registration.waiting&&navigator.serviceWorker.controller)announceUpdate();await registration.update().catch(()=>undefined);}catch(error){console.warn('[PWA V94]',error);}}
   async function install(){if(state.installed){toast('Приложение уже установлено');return;}if(deferredPrompt){deferredPrompt.prompt();await deferredPrompt.userChoice;deferredPrompt=null;return;}toast(/iphone|ipad|ipod/i.test(navigator.userAgent)?'Safari → Поделиться → На экран «Домой»':'Нажмите значок установки в адресной строке браузера');}
   async function applyUpdate(){const registration=await navigator.serviceWorker?.getRegistration?.();if(registration?.waiting){registration.waiting.postMessage({type:'SKIP_WAITING'});return;}location.reload();}
-  navigator.serviceWorker?.addEventListener?.('controllerchange',()=>{try{if(sessionStorage.getItem('sbos-v93-r1-reloaded')==='1')return;sessionStorage.setItem('sbos-v93-r1-reloaded','1');location.reload();}catch(_){location.reload();}});
+  navigator.serviceWorker?.addEventListener?.('controllerchange',()=>{try{if(sessionStorage.getItem('sbos-v94-r1-reloaded')==='1')return;sessionStorage.setItem('sbos-v94-r1-reloaded','1');location.reload();}catch(_){location.reload();}});
   window.SecondBrainPWA={install,applyUpdate,getStatus:()=>({...state,canPrompt:Boolean(deferredPrompt)})};
   window.addEventListener('load',async()=>{await register();setTimeout(()=>window.SecondBrainCloud?.init?.(),250);});
 })();
--- a/sw.js
+++ b/sw.js
@@ -1,5 +1,5 @@
-const CACHE_NAME='second-brain-os-v93-personal-assistant-r1';
-const CORE=['./offline.html','./manifest.webmanifest?v=v93-r1','./icon-192-v84.png','./icon-512-v84.png','./maskable-512-v84.png','./pwa-v88.js?v=v93-r1','./styles-v884-finance.css?v=v885-r1','./styles-v91-stability.css?v=v93-r1','./app-v91-stability.js?v=v93-r1','./styles-v93-assistant.css?v=v93-r1','./app-v93-assistant.js?v=v93-r1'];
+const CACHE_NAME='second-brain-os-v94-csv-import-restored-r1';
+const CORE=['./offline.html','./manifest.webmanifest?v=v94-r1','./icon-192-v84.png','./icon-512-v84.png','./maskable-512-v84.png','./pwa-v88.js?v=v94-r1','./styles-v884-finance.css?v=v885-r1','./styles-v91-stability.css?v=v94-r1','./app-v91-stability.js?v=v94-r1','./styles-v93-assistant.css?v=v94-r1','./app-v93-assistant.js?v=v94-r1'];
 self.addEventListener('install',event=>{self.skipWaiting();event.waitUntil(caches.open(CACHE_NAME).then(async cache=>{for(const url of CORE){try{const response=await fetch(url,{cache:'reload'});if(response.ok)await cache.put(url,response.clone())}catch(_){}}}))});
 self.addEventListener('activate',event=>{event.waitUntil((async()=>{const keys=await caches.keys();await Promise.all(keys.filter(key=>key!==CACHE_NAME).map(key=>caches.delete(key)));await self.clients.claim()})())});
 self.addEventListener('message',event=>{if(event.data?.type==='SKIP_WAITING')self.skipWaiting()});
--- a/manifest.webmanifest
+++ b/manifest.webmanifest
@@ -4,7 +4,7 @@
   "description": "Личная система финансов, привычек, целей и знаний.",
   "lang": "ru-RU",
   "id": "./?source=pwa",
-  "start_url": "./?v=93#today",
+  "start_url": "./?v=94#today",
   "scope": "./",
   "display": "standalone",
   "display_override": [
