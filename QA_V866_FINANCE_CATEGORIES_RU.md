# Проверка V86.6

Проверено:
- JavaScript проходит `node --check`;
- `index.html` и прямая версия приложения идентичны;
- HTML разбирается без потери основных script/style-блоков;
- категории расходов и доходов хранятся раздельно;
- существующие категории операций мигрируют в справочник;
- добавление пользовательской категории работает в тесте логики;
- категории расходов не попадают в список доходов;
- банковский CSV использует выпадающие списки типа и категории;
- команды очистки пользовательских коллекций не добавлялись.

Полноценный визуальный прогон Chromium в среде был заблокирован политикой запуска браузера. Поэтому после публикации рекомендуется проверить одну тестовую категорию и одну тестовую операцию перед массовым импортом CSV.
