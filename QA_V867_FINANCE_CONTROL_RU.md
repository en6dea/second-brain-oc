# Проверка V86.7

Проверено программно:
- синтаксис встроенного JavaScript через `node --check`;
- наличие функций лимитов, обязательности и финансового помощника;
- наличие месячной аналитики и сравнения периодов;
- наличие обязательности в экспорте CSV;
- отсутствие команд очистки `localStorage` в добавленной логике;
- одинаковое содержимое `index.html` и прямой страницы приложения.

Визуальный прогон Chromium в контейнере не завершился стабильно, поэтому после публикации рекомендуется сначала проверить одну тестовую категорию с небольшим лимитом и одну тестовую расходную операцию.
